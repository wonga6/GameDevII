using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Networking;

public class RandomForRestart : NetworkBehaviour {
	/*
	// Use this for initialization
	void Start () {
		NetworkServer.RegisterHandler (MsgType.SYSTEM_READY, OnPlayerReadyMessage);
	}
	
	// Update is called once per frame
	void Update () {
		
	}

	void OnPlayerReadyMessage (NetworkConnection conn, NetworkReader nr) {
		GameObject player = SpawnPlayer (Vector3.zero, "player");

		NetworkServer.ready
	}*/
}
